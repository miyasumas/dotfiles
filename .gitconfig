[include]
	path = ~/git/config

[core]
	autocrlf = input
	editor = vim

[alias]
	vacuum = "!f () { git branch --merged|grep -v '*'|xargs git branch -d; git fetch -p; };f"

[merge]
	conflictStyle = diff3
